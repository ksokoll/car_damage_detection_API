# car_damage_detection_API
An ML API that validates car damage photos for insurance claims, including quality checks, ONNX inference, DynamoDB persistence, deployed on AWS Lambda via Terraform. Built as a portfolio project, but also built to actually work.
